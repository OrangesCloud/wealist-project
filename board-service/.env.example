# ============================================================================
# Board Service - Environment Variables (wealist-project compatible)
# ============================================================================
# This file follows the wealist-project environment variable structure.
# Copy this file to .env and adjust values as needed.
#
# DUAL FORMAT SUPPORT:
# This service supports both original (wealist-project) and current format
# environment variables. When both are provided, original format takes precedence.
# ============================================================================

# ----------------------------------------------------------------------------
# Board Service Configuration
# ----------------------------------------------------------------------------
# Environment mode: "dev" or "prod"
# - dev: maps to debug mode with detailed logging
# - prod: maps to release mode optimized for production
ENV=dev

# Port to listen on (default: 8000)
SERVER_PORT=8000

# Automatic database migration on startup (placeholder for future use)
USE_AUTO_MIGRATE=true

# Log level: "debug", "info", "warn", "error"
LOG_LEVEL=debug

# Output path: "stdout", "stderr", or file path
LOG_OUTPUT_PATH=stdout

# HTTP timeouts (format: 10s, 1m, etc.)
# SERVER_READ_TIMEOUT=10s
# SERVER_WRITE_TIMEOUT=10s
# SERVER_SHUTDOWN_TIMEOUT=30s

# ----------------------------------------------------------------------------
# Database Configuration (docker-compose injected)
# ----------------------------------------------------------------------------
# PRIMARY FORMAT: PostgreSQL connection URL (original wealist-project format)
# Format: postgresql://user:password@host:port/dbname?sslmode=disable
DATABASE_URL=postgresql://board_service:board_pass12345@postgres:5432/wealist_board_db?sslmode=disable

# ALTERNATIVE FORMAT: Individual database settings (for local development)
# Uncomment these if you prefer to use individual variables instead of DATABASE_URL
# Note: If DATABASE_URL is set, it takes precedence over individual variables
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=password
# DB_NAME=project_board

# Connection pool settings
# DB_MAX_OPEN_CONNS=25
# DB_MAX_IDLE_CONNS=5
# DB_CONN_MAX_LIFETIME=5m

# ----------------------------------------------------------------------------
# Redis Configuration (placeholder for future compatibility)
# ----------------------------------------------------------------------------
# Redis connection URL (not currently used, reserved for future features)
REDIS_URL=redis://:pass12345@redis:6379/1

# ----------------------------------------------------------------------------
# JWT Configuration
# ----------------------------------------------------------------------------
# Secret key for signing JWT tokens (original wealist-project format)
# IMPORTANT: Change this in production to a strong, random secret
# Generate with: openssl rand -base64 32
SECRET_KEY=CHANGE_THIS_JWT_SECRET_KEY

# ALTERNATIVE FORMAT: Current format also supported
# JWT_SECRET=your-secret-key-change-this-in-production

# Token expiration time (format: 24h, 7d, 168h)
JWT_EXPIRE_TIME=24h

# ----------------------------------------------------------------------------
# User Service Configuration
# ----------------------------------------------------------------------------
# User service endpoint (original wealist-project format)
# Used for workspace membership validation and user profile retrieval
USER_SERVICE_URL=http://user-service:8080

# ALTERNATIVE FORMAT: Current format also supported
# USER_API_BASE_URL=http://localhost:8080

# HTTP request timeout for User API calls (format: 5s, 10s, 1m)
USER_API_TIMEOUT=5s

# ----------------------------------------------------------------------------
# CORS Configuration
# ----------------------------------------------------------------------------
# Allowed origins for CORS (comma-separated, original wealist-project format)
CORS_ORIGINS=http://localhost:3000

# ALTERNATIVE FORMAT: Current format also supported
# CORS_ALLOWED_ORIGINS=http://localhost:3000,https://app.example.com

# Allowed HTTP methods (comma-separated)
# CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE

# Allowed headers (comma-separated)
# CORS_ALLOWED_HEADERS=Origin,Content-Type,Authorization

# Allow credentials (true/false)
# CORS_ALLOW_CREDENTIALS=true
