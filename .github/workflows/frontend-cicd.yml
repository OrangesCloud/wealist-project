name: Frontend CI/CD to S3/CloudFront

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      # ============================================
      # 1. AWS Credentials 설정
      # ============================================
      - name: 🔐 Configure AWS Credentials (for all tasks)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.FRONTEND_DEPLOY_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.FRONTEND_DEPLOY_AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-2

      # ============================================
      # 2. Setup (pnpm/Node.js)
      # ============================================
      - name: ⚙️ Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8.15.0

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'
          cache-dependency-path: 'pnpm-lock.yaml'

      # ============================================
      # 3. SSM Parameter Store에서 설정 조회 및 주입
      # ============================================
      - name: 🌍 Fetch Configuration from SSM for Vite
        run: |
          echo "🌐 Fetching configuration from SSM Parameter Store..."

          # 1. ALB 도메인 조회 -> VITE_API_BASE_URL 설정
          ALB_DOMAIN=$(aws ssm get-parameter --name "/wealist/prod/alb/domain" --query "Parameter.Value" --output text)
          echo "VITE_API_BASE_URL=https://$ALB_DOMAIN" >> $GITHUB_ENV

          # ⭐️ [핵심 추가] 프론트엔드 호스트 주소 주입 (OAuth 및 절대 경로 문제 해결용)
          # USER_PUBLIC_HOST에 배포 환경 주소를 주입하여 localhost:8080 Fallback을 막습니다.
          echo "VITE_USER_PUBLIC_HOST=https://api.wealist.co.kr" >> $GITHUB_ENV 
          echo "VITE_BOARD_PUBLIC_HOST=https://api.wealist.co.kr" >> $GITHUB_ENV


          echo "✅ API Base URL: https://$ALB_DOMAIN"
          echo "✅ Public Host: https://wealist.co.kr"

      - name: 📦 Install Dependencies
        run: pnpm install --frozen-lockfile

      # ============================================
      # 4. Build with Vite
      # ============================================
      - name: 🔨 Build with Vite
        run: pnpm run build
      # ============================================
      # 5. Deploy & Invalidate Cache
      # ============================================
      - name: ⬆️ Deploy to S3 (Optimized Cache Control Applied)
        run: |
          # 1. 해시된 정적 파일: 1년 캐시 + immutable
          aws s3 sync ./dist s3://${{ secrets.FRONTEND_S3_BUCKET_NAME }}/ \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "index.html"
            
          # 2. index.html: 캐시 비활성화 (no-store, must-revalidate)
          aws s3 cp ./dist/index.html s3://${{ secrets.FRONTEND_S3_BUCKET_NAME }}/index.html \
            --cache-control "no-cache, no-store, must-revalidate" \
            --content-type "text/html"

      - name: 🌐 Invalidate CloudFront Cache (Only for index.html)
        run: |
          # CloudFront 캐시 무효화 요청을 index.html에만 적용
          aws cloudfront create-invalidation \
            --distribution-id ${{ secrets.FRONTEND_CLOUDFRONT_ID }} \
            --paths "/index.html"

      - name: ✅ Deployment Complete
        run: echo "✅ Frontend deployed successfully!"
